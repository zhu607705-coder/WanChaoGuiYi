# Domain Core

This directory is the canonical engine-free C# gameplay source for headless
simulation and command-line verification.

Files here were migrated from the former editor-bound script tree without
behavior changes. New pure-code gameplay work should update this directory
first; no active runtime should link back to the removed editor project tree.
